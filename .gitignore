venv/
.venv/
__pycache__/
.env
**/key.properties
*.jks
*.keystore
.DS_Store
*.pyc
*.pyo
.pytest_cache/
.coverage
coverage.xml
*.egg-info/
dist/
build/

# Models (GGUF files too large for git)
models/*.gguf

# Checkpoints (training artifacts, 600MB+)
checkpoints/

# Data dumps (debug scratch, not source data)
jsonFood.md

# Uploads (tmp user images)
data/uploads/
data/images/

# Logs
logs/

# Personal learning notes; intentionally kept local and not versioned.
docs/

# Internal working notes and agent memories may contain local paths, accounts,
# deployment details, or debugging context. Keep them on the developer machine.
.Codex/
.claude/
LOCAL_TESTING.md
plan.md
# Local assistant/project instructions; kept on the developer machine.
GLOSSARY.md
AGENTS.md
CLAUDE.md

# RAG eval reports (output, không commit — chạy lại sinh mới)
ml/evaluation/reports/
reports/

# Dart defines (chứa client ID thật — dùng dart_defines.example.json làm mẫu)
mobile/dart_defines.json

backups/
generated_sprites/
