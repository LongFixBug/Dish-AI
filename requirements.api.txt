# Runtime dependencies for the FastAPI image only.
# Training, Streamlit UI, and RAGAS evaluation run outside the API container.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.0.0
httpx>=0.27.0
sqlalchemy[asyncio]>=2.0.51
asyncpg>=0.31.0
# Historical Alembic revisions require the type adapter; runtime vectors use Qdrant.
pgvector>=0.5.0
python-multipart>=0.0.32
qdrant-client>=1.18.0,<2
